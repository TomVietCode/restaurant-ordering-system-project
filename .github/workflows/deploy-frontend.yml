name: Build and Push Frontend Docker image to GHCR

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: ./code/frontend/web-admin
          file: ./code/frontend/web-admin/Dockerfile
          push: true
          tags: |
            ghcr.io/tomvietcode/ioc-frontend:latest
            ghcr.io/tomvietcode/ioc-frontend:${{ github.ref_name }}
          build-args: |
            NEXT_PUBLIC_API_URL=/api

      - name: Output image name
        run: |
          echo "Image pushed: ghcr.io/tomvietcode/ioc-frontend:latest"
